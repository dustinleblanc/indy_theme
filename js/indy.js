/**
 * @file
 * Indy behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.indy = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
